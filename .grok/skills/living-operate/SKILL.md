---
name: living-operate
description: Operate living-core densest process. Prefer Grok-native skill procedure + thin MCP hands (sense, rankCycle, skill, ranking, handoff). Use when operate, hop0, living-core, rankCycle, or REVIEW.
---

# living-operate (Grok Skill — harness)

## Law (Anthropic 2025–26 densest)

| Layer | Role | living-core |
|-------|------|-------------|
| **MCP** | Hands — tool connectivity | thin `living_*` core tools |
| **Skills** | Judgment — how to work | **this file** + `living_skill` packages |
| **Pages** | Memory — notes own | `store/pages` · hop0 · handoff |

MCP is **not** the full picture. Flat tool dumps tax context. Progressive disclosure:

1. Skill metadata / hop0 `skills=` (tiny)
2. Skill body JIT (`living_skill get` or this SKILL.md)
3. MCP tool only when hands needed

## Steps densest

1. Read this skill (already loaded if triggered) — **do not** re-list all MCP tools.
2. `living_sense` (host) — `open_next=` · `forecast=` · `skills=` · `session=` · `last_capture=` · `perf=` · debt.
3. Branch on `open_next`:
   - `calendar_debt` → Best calendar_layers / rank toward calendar
   - `REVIEW …` → `living_skill / living_rank_cycle` · approve **only if intentional**
   - `research_debt` → Best research / densest findings
   - `operate_close` → `living_rank_cycle` or user densest help (no roadmap farm)
4. Process re-enter → `living_skill action=list` then `get id=…` (not full HOW dump).
5. Session re-enter → `living_token_view action=handoff modality=…` (not catalog prefill).
6. After code edits → `living_reload` · `node scripts/smoke.js` if runtime/MCP changed.
7. Never invent joy values. REVIEW stays human.

## hop0 re-enter (P73–P76 polish)

| Signal | Meaning |
|--------|---------|
| `session=` | last-K Best densest (survives MCP reload) |
| `last_capture=` | **captures_tail SoT** · ISO at (not stale loop) |
| `perf=` | stage ms sense/sim/explore/best |
| `related=` | operate densest edges preferred over pure law triad |
| createRuntime | densest wire ~351 · not feature farm |

## Prefer

- **Skill procedure first**, MCP second
- One-shot `living_rank_cycle` over many tiny loop tools
- `living_tool_help action=core|get` for rare tools (list defaults **core**)
- AGENTS.md law over re-explaining

## MCP list modes (init hang fix)

| Env | Effect |
|-----|--------|
| default **core** | ~9 operate tools — fast init / low context |
| `LIVING_MCP_LIST=dense` | all tools, short schemas |
| `LIVING_MCP_LIST=full` | verbose schemas |

If MCP stuck on init: restart living server; prefer core list (default).

## Do not

- Rebuild sessions/memory/editor inside living-core
- Farm roadmap without hop0 debt
- Treat MCP as procedure library (skills own that)
- Open Mac apps unless `living_invoke` intentional (`dry_run` first)

## Links

- store/pages/research_skills_vs_mcp.md
- store/pages/operate_close.md
- store/pages/operate_runtime.md
- store/pages/operate_handoff.md
- store/pages/operate_skills.md
