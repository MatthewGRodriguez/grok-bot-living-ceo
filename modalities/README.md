# modalities

Start here: [`host/docs/HOW.md`](host/docs/HOW.md). That is the modality that explains modalities.

```
joy
 └── host
      ├── data
      │    ├── pages
      │    ├── exports
      │    └── samples
      ├── research
      ├── crystallize
      └── craft

joy
 └── ceo_next     later decision node (excerpt)
```

Each package is `MANIFEST.json` + `lambda/index.js` + docs. Best enters one sibling under the parent goal.

`EXTERNALS.md` is a live explore dump. It is not shipped here (no machine fingerprint).

Law and jmethod: [docs/modalities.md](../docs/modalities.md).
Features those modalities exist for: [docs/living-core-features.md](../docs/living-core-features.md).
