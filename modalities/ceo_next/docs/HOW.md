# modality:ceo_next — in-rank what-next (excerpt)

Child of **joy**. Scores act-children and writes the Best pick.

This public slice ships the lambda only as an excerpt.

- `effectiveness({ simulated: true })` loads.
- `work()` wants `store/pages/` and sibling modalities (`ceo_mgmt_self`, `ceo_play_adonia`, `ceo_encode_priors`, `ceo_exo_gen`) that are not here. Those children fall back to a default score.

Not part of the bootstrap tree. See [host HOW](../host/docs/HOW.md) for how modalities work.
